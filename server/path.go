package server

const (
	rootPath = "/v1"

	RootAccount   = rootPath + "/accounts"
	RootPerson    = rootPath + "/persons"
	RootPublisher = rootPath + "/publishers"
	RootAuthor    = rootPath + "/author"
	RootBook      = rootPath + "/book"
	RootBorrowing = rootPath + "/borrow"

	PathLogin = "/login"
)
